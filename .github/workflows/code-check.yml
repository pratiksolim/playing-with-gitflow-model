name: Test code check

on:
    pull_request:
        branches:
            - 'releases/npi-base-latest'
            - 'releases/npi-base-previous'
    workflow_call:

jobs:
    test_check:
        name: 🔎
        runs-on: ubuntu-latest
        steps:
            - run: echo "All checks passed successfully!"